#include <stdio.h>
#define max 10000
#define bigger(a,b) a>b? a:b

int n,k;
int use[max];
int coin[max];
int box(){

    scanf("%d %d", &n , &k);
    int cnt =0;
    for(int i =1; i<=n; i++)
        scanf("%d", &coin[i]);
    for(int i =1; i<=n; i++)
    {
        for()
    }

}


int main()
{
  
}